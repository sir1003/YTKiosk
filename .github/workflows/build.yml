name: Build YTKiosk IPA

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Theos
        run: |
          bash -c "$(curl -fsSL https://raw.githubusercontent.com/theos/theos/master/bin/install-theos)"
          echo "THEOS=$HOME/theos" >> $GITHUB_ENV

      - name: Download iOS SDK
        run: |
          git clone --depth=1 --filter=blob:none --sparse https://github.com/theos/sdks.git $HOME/theos-sdks
          cd $HOME/theos-sdks
          git sparse-checkout set iPhoneOS9.3.sdk iPhoneOS14.4.sdk iPhoneOS16.5.sdk
          cp -r iPhoneOS*.sdk $HOME/theos/sdks/ || true

      - name: Build IPA
        run: |
          ls -la
          make package THEOS=$HOME/theos
        
      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: YTKiosk-IPA
          path: YTKiosk.ipa
